import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kindness.serviceconnect',
  appName: 'Service Connect Pro',
  webDir: 'dist'
};

export default config;
